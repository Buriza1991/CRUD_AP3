{
  "name": "crud-ap3",
  "version": "1.0.0",
  "description": "Sistema CRUD com DevOps Integration - Projeto AP3",
  "private": true,
  "scripts": {
    "install-deps": "npm install --prefix frontend && npm install --prefix backend",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:frontend": "npm start --prefix frontend",
    "start:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "test": "npm test --prefix frontend && npm test --prefix backend",
    "setup": "npm run install-deps",
    "dev": "npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
} 